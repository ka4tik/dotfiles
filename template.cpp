/*
 *Kartik Singal @ ka4tik
 */
#include<bits/stdc++.h>
using namespace std;
#define s(n) scanf("%d",&n);
int main()
{
    //freopen("in","r",stdin);

    int N;
    s(N);
    vector<int> v(N);
    for(int i=0;i<N;i++)
    {
        s(v[i]);
    }

    return 0;
}
